{
  "name": "cms",
  "private": true,
  "version": "0.1.0",
  "description": "A Strapi application",
  "scripts": {
    "develop": "NODE_ENV=local-dev ENV_PATH=./.env.local-dev strapi build && NODE_ENV=local-dev ENV_PATH=./.env.local-dev strapi develop",
    "offline-serve-admin": "NODE_ENV=local ENV_PATH=./.env.local strapi build && NODE_ENV=local ENV_PATH=./.env.local serve ./build -p 5000",
    "offline-serve": "serverless offline --stage=local",
    "build": "NODE_ENV=production ENV_PATH=./.env.production strapi build"
  },
  "devDependencies": {
    "serverless-dotenv-plugin": "^4.0.2"
  },
  "dependencies": {
    "@strapi/plugin-i18n": "4.5.0",
    "@strapi/plugin-users-permissions": "4.5.0",
    "@strapi/strapi": "4.5.0",
    "better-sqlite3": "7.4.6",
    "serverless-http": "^3.1.0",
    "serverless-scriptable-plugin": "^1.2.2"
  },
  "author": {
    "name": "A Strapi developer"
  },
  "strapi": {
    "uuid": "3b589528-cdbe-48c7-a447-43fed3e7abc8"
  },
  "engines": {
    "node": ">=14.19.1 <=18.x.x",
    "npm": ">=6.0.0"
  },
  "license": "MIT"
}
